import { StarFillingPipe } from './star-filling.pipe';

describe('StarFillingPipe', () => {
  it('create an instance', () => {
    const pipe = new StarFillingPipe();
    expect(pipe).toBeTruthy();
  });
});
